import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ashwani.road.rush',
  appName: '3D Road Rush',
  webDir: 'www/browser',
};

export default config;
